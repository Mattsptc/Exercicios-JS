const resultado = nota => nota >= 7 ? "Aprovado" : "Reprovado";
/* const resultado = nota => {
     return nota >= 7 ? "Aprovado" : "Reprovado"
} Funcionaria igualmente como a funcao acima */

console.log(resultado(7.1));
console.log(resultado(5.2));

